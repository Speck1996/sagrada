package com.model.dice;

import org.junit.Assert;
import org.junit.Test;

import java.util.ArrayList;

public class StockTest {
  /*  @Test
    public void testGetter(){
        ArrayList<Dice> list = new ArrayList<>();
        Stock dices = new Stock();
        Dice dice1 = new Dice(DiceColor.BLUE, DiceShade.FIVE);
        Dice dice2 = new Dice(DiceColor.YELLOW, DiceShade.ONE);
        Dice dice3 = new Dice(DiceColor.RED, DiceShade.SIX);
        list.add(dice1);
        list.add(dice2);
        list.add(dice3);
        Assert.assertEquals(dices.getDice(),list);
    }

    @Test
    public void testPickDice() throws NoDiceException{
        int index = 0;
        ArrayList<Dice> list = new ArrayList<>();
        Stock dices = new Stock();
        Dice dice1 = new Dice(DiceColor.BLUE, DiceShade.FIVE);
        Dice dice2 = new Dice(DiceColor.YELLOW, DiceShade.ONE);
        Dice dice3 = new Dice(DiceColor.RED, DiceShade.SIX);
        list.add(dice1);
        list.add(dice2);
        list.add(dice3);
        Dice picked = dices.removeDice(index);
        Assert.assertEquals(picked,dice1);
        Assert.assertEquals(dices.getDice().size(),2);
        Assert.assertEquals(dices.getDice().get(0),dice2);
        Assert.assertEquals(dices.getDice().get(1),dice3);
    }

    @Test
    public void testInsertDice(){
        ArrayList<Dice> list = new ArrayList<>();
        Stock dices = new Stock();
        Dice dice1 = new Dice(DiceColor.BLUE, DiceShade.FIVE);
        dices.insertDice(dice1);
        Assert.assertEquals(dices.getDice().get(0),dice1);
        Assert.assertEquals(dices.getDice().size(),1);
    }*/
}
